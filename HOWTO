
Install it locally (dev):
cp extension.js metadata.json ~/.local/share/gnome-shell/extensions/xorg-or-wayland@soulserv.net

Package it for publishing to gnome.org:
rm extension.zip && zip -j extension.zip metadata.json extension.js

Publish to:
https://extensions.gnome.org/upload/

